---
interface Props {
  href?: string;
  variant?: 'primary' | 'secondary' | 'whatsapp' | 'outline' | 'ghost' | 'glass';
  size?: 'sm' | 'md' | 'lg' | 'xl';
  class?: string;
  dataCta?: string;
  fullWidth?: boolean;
  [key: string]: any;
}

const {
  href,
  variant = 'primary',
  size = 'md',
  class: className = '',
  dataCta,
  fullWidth = false,
  ...rest
} = Astro.props;

const Tag = href ? 'a' : 'button';
---

<Tag
  href={href}
  class:list={['btn', `btn--${variant}`, `btn--${size}`, { 'btn--full': fullWidth }, className]}
  data-cta={dataCta}
  {...rest}
>
  <span class="btn__inner"><slot /></span>
</Tag>

<style>
  .btn {
    --btn-bg: var(--accent);
    --btn-color: var(--text-white);
    --btn-shadow: 0 8px 24px rgba(255, 107, 53, 0.28);
    --btn-shadow-hover: 0 14px 36px rgba(255, 107, 53, 0.38);

    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    font-family: var(--font);
    font-weight: 600;
    letter-spacing: -0.01em;
    border-radius: var(--radius-md);
    text-decoration: none;
    white-space: nowrap;
    cursor: pointer;
    border: none;
    position: relative;
    overflow: hidden;
    isolation: isolate;
    background: var(--btn-bg);
    color: var(--btn-color);
    box-shadow: var(--btn-shadow);
    transition:
      transform 0.35s var(--ease-out-back),
      box-shadow 0.35s var(--ease-out-expo),
      background 0.25s ease,
      color 0.25s ease;
  }
  .btn__inner {
    position: relative;
    z-index: 2;
    display: inline-flex;
    align-items: center;
    gap: 10px;
  }
  .btn::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(120deg, transparent 25%, rgba(255,255,255,0.22) 50%, transparent 75%);
    transform: translateX(-120%);
    transition: transform 0.7s var(--ease-out-expo);
    z-index: 1;
  }
  .btn:hover::before { transform: translateX(120%); }
  .btn:hover { transform: translateY(-3px); box-shadow: var(--btn-shadow-hover); }
  .btn:active { transform: translateY(-1px); }
  .btn--full { width: 100%; }

  /* Sizes */
  .btn--sm  { font-size: 13px; padding: 9px 16px;  border-radius: var(--radius-sm); }
  .btn--md  { font-size: 15px; padding: 13px 22px; }
  .btn--lg  { font-size: 16px; padding: 16px 28px; }
  .btn--xl  { font-size: 17px; padding: 20px 32px; border-radius: var(--radius-lg); }

  /* Variants */
  .btn--primary {
    background: var(--grad-accent);
  }

  .btn--secondary {
    --btn-bg: var(--primary);
    --btn-color: #FFFFFF;
    --btn-shadow: 0 8px 24px rgba(30, 58, 95, 0.30);
    --btn-shadow-hover: 0 14px 36px rgba(30, 58, 95, 0.40);
    background: var(--grad-primary);
  }

  .btn--whatsapp {
    --btn-bg: var(--whatsapp-green);
    --btn-shadow: 0 8px 24px rgba(37, 211, 102, 0.30);
    --btn-shadow-hover: 0 14px 36px rgba(37, 211, 102, 0.42);
    background: linear-gradient(135deg, #2EE072 0%, #25D366 50%, #1FB855 100%);
  }

  .btn--outline {
    --btn-bg: transparent;
    --btn-color: var(--accent);
    --btn-shadow: none;
    --btn-shadow-hover: 0 8px 24px rgba(255, 107, 53, 0.18);
    background: transparent;
    border: 1.5px solid var(--accent);
  }
  .btn--outline:hover {
    background: var(--accent);
    color: var(--text-white);
  }

  .btn--ghost {
    --btn-bg: transparent;
    --btn-color: var(--text-primary);
    --btn-shadow: none;
    --btn-shadow-hover: none;
    background: transparent;
    border: 1.5px solid var(--border-medium);
  }
  .btn--ghost:hover {
    background: var(--bg-soft);
    border-color: var(--primary);
    color: var(--primary);
  }

  .btn--glass {
    --btn-bg: rgba(255, 255, 255, 0.08);
    --btn-color: #FFFFFF;
    --btn-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.18);
    --btn-shadow-hover: inset 0 0 0 1px rgba(255, 255, 255, 0.32), 0 8px 24px rgba(0,0,0,0.18);
    background: rgba(255, 255, 255, 0.08);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
  }
  .btn--glass:hover {
    background: rgba(255, 255, 255, 0.14);
  }
</style>
